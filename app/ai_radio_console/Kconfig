#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config LVX_USE_DEMO_CONTEST2026_095_AI_RADIO_CONSOLE
	bool "Contest 2026 team 095 DayNote - Daily Voice Notes"
	default n
	---help---
		DayNote: voice memo device for the full day.
		Hold-to-record -> WAV -> ASR (SiliconFlow) -> LLM summary -> highlight extraction.
		Stores notes on local filesystem. 2.8" resistive touchscreen + LRADC button.
