# 行动规划指导方针

> 本文档时刻指导工作方向，防止丢上下文。每完成一项打勾，每开始一项更新状态。

---

## 项目目标

为 Gemini-S1 (R528) 开发板构建 AI 电台控制台固件，实现：
1. 录音 → ASR → AI 整理（得到大脑式语音笔记）
2. LVGL UI 在 320x240 SPI LCD 上显示
3. 可刷写到实体机并正常运行
4. 同时参加小米 openvela 比赛 + 哈工大空间通信比赛

## 核心原则

1. **先让它能跑** — 先修复致命问题，让屏幕亮起来、音频能采集
2. **再让它好用** — 然后优化 ASR、AI 整理、UI 体验
3. **最后做锦上添花** — CW 解码、频谱分析、MAYDAY 检测等高级功能
4. **每次修改后验证** — 不要一次改太多，改一个验证一个
5. **文档跟上代码** — 每修一个功能就更新对应文档

## 当前状态

- ✅ openvela 源码树已同步 (`/home/bi4mib/openvela-build/`)
- ✅ 固件已编译 (nuttx.bin 108MB, vela.bin 5.7MB)
- ✅ AI Radio Console 已启用在 .config 中
- ❌ I2S 和 LRADC 被禁用 — 音频采集和按键不工作
- ❌ 未打包成 .img — 无法刷写
- ❌ PTT 硬件引脚错误 — GPADC0 不能驱动 PTT
- ❌ 多个代码质量问题

## 工作流程

```
修复问题 → menuconfig 配置 → 编译验证 → 打包刷写 → 实机测试
```

## 决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-07-10 | 优先修复 I2S/LRADC 配置 | 没有音频采集和按键，核心功能无法工作 |
| 2026-07-10 | 优先修复命令注入 | 安全漏洞，且修复简单 |
| 2026-07-10 | PTT 引脚问题暂不急 | 需要硬件改板，先用软件 PTT 按钮 |
| 2026-07-10 | 不依赖 ai_agent | 竞赛版是独立应用，不走 agent IPC |
